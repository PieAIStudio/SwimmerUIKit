# Historical liquid review captures — retained, not scratch space

These six PNGs were committed by the SwimmerUIKit owner in `4d4b90d`
(`feat(LiquidGroup): add stroke support via SVG filters`, 2026-08-28): merging,
separated and reduced-motion states, each on desktop and mobile.

They belong to the historical stroke/merge review, not the live test suite.
Their ideal archival classification is visual evidence, but their old paths may
be used by external review threads that this audit cannot enumerate. Keep all
six bytes and paths, and use this label rather than silently moving/deleting
them. No new temporary output belongs here: use `.devspace-visual/`.
